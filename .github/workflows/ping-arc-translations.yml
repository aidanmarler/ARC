name: Notify ARC-Translations repo

on:
  push:
      branches:
       - main
  workflow_dispatch:

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger ARC-Translations update
        run: |
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.ARC_TRANSLATIONS_DISPATCH_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/ISARICResearch/ARC-Translations/dispatches \
            -d '{"event_type": "arc-updated"}'
